<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.linhnv.fbee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".MainActivity" />
        <activity android:name=".TestAndGiftActivity" />
        <activity android:name=".TryUploadActivity" />
        <activity android:name=".QuizOccupationActivity" />
        <activity android:name=".QuizPersonalityActivity" />
        <activity android:name=".QuizResultActivity" />
        <activity android:name=".GetGiftActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".UpdateInfoActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".ChangeProfileActivity" />
        <activity android:name=".GiftActivity" />
        <activity android:name=".ControllerActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".QuizResultUserPersonalityActivity"></activity>
    </application>

</manifest>